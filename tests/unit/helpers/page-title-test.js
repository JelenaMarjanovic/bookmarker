import { pageTitle } from "bookmarker/helpers/page-title";
import { module, test } from "qunit";

module("Unit | Helper | page title");

// Replace this with your real tests.
test("it works", function (assert) {
  let result = pageTitle([42]);
  assert.notOk(result);
});
